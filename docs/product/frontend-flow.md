# Frontend Flow

## User Interactions

1. Select Start mode → click map → place start marker
2. Select End mode → click map → place end marker
3. Click Find Shortest Path → call API → draw polyline
4. Click Clear → reset all

## Components

- MapView (Leaflet map + markers + polyline)
- RouteControls (buttons)
